Logs a line when a request is received and then another when the corresponding
response is sent.

Each request is given a unique id so the two lines can be associated with each
other.

See `example.js` for usage
